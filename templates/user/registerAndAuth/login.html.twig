{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
    <form method="post">

        {% if error %}
            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}


        <div>
            <h1>Connectez-vous</h1>

            <p>{{ emailEntered }}</p>
        </div>


        <div>
            <label for="inputPassword">Password</label>
            <input type="password" name="password" id="inputPassword" autocomplete="current-password" required autofocus>
        </div>

        <div>
            <p>
                <a href="#">Mot de passe oublié</a>
            </p>
        </div>


        <input type="hidden" name="_csrf_token"
               value="{{ csrf_token('authenticate') }}"
        >

        <div>
            <label>
                <input type="checkbox" name="_remember_me" checked="checked"> Se souvenir de moi
            </label>
        </div>

        <button type="submit">
            Sign in
        </button>
    </form>
{% endblock %}
